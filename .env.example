# Public HTTPS origin of wallet-server. Leave empty only when a reverse proxy
# serves the Console and /custody from the same origin.
VITE_CUSTODY_API_BASE=https://wallet.example.com

# Display label only; it does not select backend networks.
VITE_CUSTODY_ENVIRONMENT=Production
